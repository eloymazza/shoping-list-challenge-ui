import { styled } from "@mui/material";

export const StyledItemsListHeader = styled("header")({
  display: "flex",
  justifyContent: "space-between",
  alignItems: "center",
  marginBottom: "1rem",
});
